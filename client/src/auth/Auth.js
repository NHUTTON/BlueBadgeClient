import React from 'react';

import Login from  './Login';
import Register from  './Register';

const Auth = (props) => {
    return(
        <div>
            <Login />
            <Register />
        </div>
    )
}

export default Auth